# Core
anthropic>=0.39.0        # LLM content generation (Claude). Optional at runtime.
requests>=2.31.0         # HubSpot REST calls
python-dotenv>=1.0.0     # Load .env config

# Dashboard (bonus)
Flask>=3.0.0

# CLI niceties
tabulate>=0.9.0          # Pretty terminal tables for the performance report
